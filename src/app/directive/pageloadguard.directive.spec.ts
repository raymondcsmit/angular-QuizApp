import { PageloadguardDirective } from './pageloadguard.directive';

describe('PageloadguardDirective', () => {
  it('should create an instance', () => {
    const directive = new PageloadguardDirective();
    expect(directive).toBeTruthy();
  });
});
